Created Week_5_Module_6.cpp
Created Week_5_Module_11.cpp

Week_5_Module_6.cpp main calls class Item and creates an object Tool with string name = "Hammer" and int quantity = 8. It then calls method saveToFile to open items.txt and writes "Hammer,8", enters a new line then  closes items.txt, then it returns "Item saved to file." in the console and enters a new line. If the file does not open, it will return "Unable to open file for writing." then entering a new line. Then the code calls method loadFromFile to open items.txt and reads the file, outputting in the console "File content: Hammer,8" then entering a new line, then closing items.txt. The code returns 0.

Week_5_Module_11.cpp main calls class Product and creates object p with int id = 88, string name = Mechanical Pencil, float price = 4.99. The code prints "Constructor called" and enters new line. The code then prints "ID: 101, Name: Notebook, Price: $4.99", and enters a new line. Then the destructor in the Product class is called and it prints "Destructor called" and enters a new line. The code returns 0.
